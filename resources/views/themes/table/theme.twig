<strong>{{ trans(entry.name) }}</strong>

{% if entry.active %}
    <small>
        <span class="label label-success">{{ trans('module::admin.active') }}</span>
    </small>
{% endif %}

{% if entry.admin %}
    <small>
        <span class="label label-primary">{{ trans('module::admin.admin') }}</span>
    </small>
{% endif %}

<br>

<small>{{ trans(entry.description) }}</small>
